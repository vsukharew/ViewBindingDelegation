ext {
    versions = [
            androidx: [
                    'appCompat': '1.3.1',
            ]
    ]
    androidx = [
            appCompat: "androidx.appcompat:appcompat:${versions.androidx.appCompat}"
    ]
}